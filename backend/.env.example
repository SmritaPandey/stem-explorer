# Supabase Configuration (Backend)
SUPABASE_URL=""
SUPABASE_ANON_KEY="" # Used for the default Supabase client, RLS protects data access
SUPABASE_SERVICE_ROLE_KEY="" # For admin tasks or bypassing RLS when necessary (e.g., certain cron jobs, migrations)

# Storage Configuration
SUPABASE_STORAGE_MATERIALS_BUCKET="course_materials_bucket" # Example bucket name, ensure this exists in your Supabase project

# Backend Server Configuration (already in root .env.example, but can be specific here too)
# PORT=3001
# NODE_ENV=development

# Stripe Configuration (already in root .env.example, but if backend needs it directly)
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# CORS Configuration
# FRONTEND_URL=http://localhost:3000
